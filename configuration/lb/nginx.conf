
worker_processes  1;


events {
    worker_connections  1024;
}


http {

	upstream backend {
        server web;
    }

    server {

        location / {
            proxy_pass http://backend;
        }
    }
}